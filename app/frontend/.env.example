# Copy to .env.local. No trailing slash.
#
# Locally this is the monolith on your own machine. When you deploy, set this
# in the Amplify console to your EC2 address instead -- it is the only thing
# the frontend needs to know about where the backend lives.
VITE_API_BASE=http://localhost:8000
